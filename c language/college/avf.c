// //Program to read three test scores of a student and find the average of best two scores.Assume a test of maximum 25 marks each.

// #include <stdio.h>
// int main(){
//     int a,b,c;
//     float avg;
//     printf("Enter the marks of the student:\n");
//     scanf("%d%d%d",&a,&b,&c);
//     if(a<=25 && b<=25 && c<=25 && a>=0 && b>=0 && c>=0)
//     {
//         if(a>c && b>c)
//         {
//             avg=(a+b)/2.0;
//             printf("Average of two best scores is : %.3f",avg);
//         }
//         else if (a>b && c>b){
//             avg=(a+c)/2.0;
//             printf("Average of two best scores is : %.3f",avg);
//         }
//         else {
//             avg=(b+c)/2.0;
//             printf("Average of two best scores is : %.3f",avg);
//         }}
//         return 0;}
#include  <stdio.h>
#include <math.h>
int main(){
    float sum=0;
    // int x,y,a,b;
    // scanf("%d%d%d%d",&x,&y,&a,&b);
    // sum=(6*x+2*y)/(a-b);
    // printf("%f",sum);
    sum=log(1);
    printf("\n %f",sum);
    return 0;}